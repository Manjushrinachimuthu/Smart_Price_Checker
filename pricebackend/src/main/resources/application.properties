spring.application.name=pricebackend

# DATABASE CONFIGURATION
spring.datasource.url=jdbc:mysql://localhost:3306/price_comparison_db
spring.datasource.username=${DB_USER:root}
# When running locally set DB_PASSWORD (or leave blank for in-memory data); never commit secrets and always supply DB_PASSWORD for production deployments.
spring.datasource.password=${DB_PASSWORD:manju@282005}

# JPA SETTINGS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# SERVER PORT
server.port=8080

# Price checker backend settings
pricesage.data-dir=./data
pricesage.compare-cache-ttl-ms=180000
pricesage.tracking-refresh-ms=1200000
pricesage.serp-api-key=4c0565b5c44e0a8719508175a4997c5b675d1eb9940da68b4777fbddf69ae746
